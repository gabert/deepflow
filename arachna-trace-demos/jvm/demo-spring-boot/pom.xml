<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.5</version>
        <relativePath/>
    </parent>

    <groupId>com.github.gabert</groupId>
    <artifactId>ArachnaTraceDemoSpringBoot</artifactId>
    <version>0.0.1-SNAPSHOT</version>

    <properties>
        <java.version>17</java.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!--
            This demo uses two SPI reference impls from
            arachna-trace-jvm-extensions/. Each one is a self-contained
            single-class plugin; we just put the JARs on the classpath
            and activate them via arachna-agent.cfg
            (`session_resolver=spring-session` and
            `jpa_proxy_resolver=hibernate`).
        -->
        <dependency>
            <groupId>com.github.gabert</groupId>
            <artifactId>SessionResolverSpring</artifactId>
            <version>0.0.1-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>com.github.gabert</groupId>
            <artifactId>JpaProxyResolverHibernate</artifactId>
            <version>0.0.1-SNAPSHOT</version>
        </dependency>
        <!--
            Third SPI impl, used by audit-demo.sh: the config resolver
            reads a fixed session_id from the agent args, so each recorded
            run gets a predictable session id the script can hand to the
            behavior-diff API. Activated per-run via inline agent config
            (`session_resolver=config&session_id=...`); the default
            arachna-agent.cfg keeps using spring-session.
        -->
        <dependency>
            <groupId>com.github.gabert</groupId>
            <artifactId>SessionResolverConfig</artifactId>
            <version>0.0.1-SNAPSHOT</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>
